spring:
  datasource:
    url: jdbc:mysql://localhost:3306/superprof_db_security?useSSL=false&createDatabaseIfNotExist=true
    username: root
    password:
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
    hibernate:
      ddl-auto: update

pedro:
  vars:
    jwtSecret: ======================BezKoder=Spring===========================
    jwtExpirationMs: 86400000
